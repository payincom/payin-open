/**
 * Type declarations for @fontsource packages
 * These packages don't have built-in TypeScript declarations
 */

declare module '@fontsource/offside' {
  const content: any;
  export default content;
}
